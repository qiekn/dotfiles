## Compact Instructions

When compressing, preserve in priority order:

1. Architecture decisions (NEVER summarize)
2. Modified files and their key changes
3. Current verification status (pass/fail)
4. Open TODOs and rollback notes
5. Tool outputs (can delete, keep pass/fail only)

## Git

when you git commit, check `git-commit-message` skill

## Env Path

> I'm using MSYS2 UCRT64 on x86_64 windows

- python: C:\Python314\python.exe
- pnpm: C:\Users\user\AppData\Roaming\npm

If some program not found, check my zsh config

- C:\msys64\home\user\.dotfiles\.config\zsh\core\env.zsh
- C:\msys64\home\user\.dotfiles\.config\zsh\modules\msys2\env.zsh
