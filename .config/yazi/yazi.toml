# "$schema" = "https://yazi-rs.github.io/schemas/yazi.json"
# Keep Yazi's built-in defaults and only override the editor.

[opener]
edit = [
	{ run = 'nvim "$@"', desc = "nvim", block = true, for = "unix" },
	{ run = 'nvim %*',   desc = "nvim", block = true, for = "windows" },
]
